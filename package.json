{
  "name": "readme-generator",
  "version": "0.1.0",
  "description": "It is a node application that creates a readme file based on user inputs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "inquirer": "^7.1.0",
    "util": "^0.12.3"
  }
}
